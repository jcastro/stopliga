services:
  stopliga:
    image: bluepr0/stopliga:latest
    container_name: stopliga
    restart: unless-stopped
    env_file:
      - .env
    command: ["--loop"]
    volumes:
      - stopliga_data:/data
      - ./secrets:/run/secrets:ro
    healthcheck:
      disable: true

volumes:
  stopliga_data:
